{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for pi-viewer main window with minimal core permissions",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
